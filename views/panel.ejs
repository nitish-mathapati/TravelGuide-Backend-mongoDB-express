<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PANEL</title>
    <link rel="stylesheet" href="/style.css"> <!-- Link to your CSS file -->
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="container w-full h-screen bg-zinc-900 text-black p-10">
        <div class="box">
            <h2>PANEL</h2>
            <p>Please choose an option:</p>
            <div class="button-group">
                <a href="/" class="button">User</a>
                <a onclick="checkAdmin()" href="/city" class="button">Admin</a>
            </div>
        </div>
    </div>
    <!-- <script>
        function checkAdmin() {
            let password;
            while (true) {
                password = prompt("Enter Admin Password:");
                
                if (password === null) {
                    return;
                }

                if (password === "nitima") {
                    window.location.href = "/city";
                    break;
                } else {
                    alert("Incorrect password! Try again.");
                }
            }
        }
    </script> -->

    <script>
        function checkAdmin() {
            let password = "";
            while (password !== "nitima") {
                password = prompt("Enter Admin Password:");
                if (password === null) return;
                // if (password.trim() === "") {
                //     alert("Password cannot be empty! Try again.");
                //     continue;
                // }
                if (password !== "nitima") {
                    alert("Incorrect password! Try again.");
                }
            }
            window.location.href = "/city";
        }
    </script>

    <!-- <script>
        function checkAdmin() {
            let password = prompt("Enter Admin Password:");

            if (password === null || password.trim() === "") {
                location.reload();
            } else if (password.trim() === "nitima") {
                window.location.href = "/city";
            } else {
                alert("Incorrect password! Try again.");
                location.reload();
            }
        }
    </script> -->
</body>
</html>